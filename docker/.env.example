# Bytebot Docker Environment Configuration
# Copy this file to .env and fill in the required values
# Generate secure passwords with: openssl rand -base64 32

# =============================================================================
# Database Configuration (REQUIRED)
# =============================================================================

# PostgreSQL password - REQUIRED, must be set before running docker compose
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=

# Optional: customize database name and user
# POSTGRES_USER=postgres
# POSTGRES_DB=bytebotdb

# =============================================================================
# API Keys (at least one required for LLM functionality)
# =============================================================================

# Anthropic Claude API key
ANTHROPIC_API_KEY=

# OpenAI API key
OPENAI_API_KEY=

# Google Gemini API key
GEMINI_API_KEY=

# =============================================================================
# Optional Configuration
# =============================================================================

# Override default service URLs (usually not needed)
# BYTEBOT_DESKTOP_BASE_URL=http://bytebot-desktop:9990
# BYTEBOT_AGENT_BASE_URL=http://bytebot-agent:9991
# BYTEBOT_DESKTOP_VNC_URL=http://bytebot-desktop:9990/websockify
# BYTEBOT_LLM_PROXY_URL=http://bytebot-llm-proxy:4000

# Override database URL (usually auto-constructed from POSTGRES_* vars)
# DATABASE_URL=postgresql://postgres:yourpassword@postgres:5432/bytebotdb
