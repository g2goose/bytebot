name: Build Agent CC

on:
  push:
    branches:
      - main
    paths:
      - "packages/bytebot-agent-cc/**"
      - "packages/shared/**"

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/bytebot-ai/bytebot-agent-cc
          tags: type=edge
          labels: |
            org.opencontainers.image.title=bytebot-agent-cc
            org.opencontainers.image.description=Bytebot Agent Claude Code (Distroless)

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        env:
          BUILDX_NO_DEFAULT_ATTESTATIONS: 1
          DOCKER_BUILD_SUMMARY: false
          DOCKER_BUILD_RECORD_UPLOAD: false
        with:
          context: ./packages
          file: ./packages/bytebot-agent-cc/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          cache-from: type=gha,scope=agent-cc
          cache-to: type=gha,mode=max,scope=agent-cc
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
